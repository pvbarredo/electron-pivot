{
  "name": "electron-pivot",
  "version": "1.0.0",
  "description": "Electron application that reminds to update tasks",
  "main": "src/electron.js",
  "scripts": {
    "start": "electron .",
    "electron:dev": "npx electronmon .",
    "electron:build": "electron-builder build --win --publish never ",
    "electron:deploy": "electron-builder build --win --publish always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Barredo",
  "license": "ISC",
  "dependencies": {
    "auto-launch": "^5.0.5",
    "electron-is-dev": "^2.0.0",
    "electron-log": "^5.0.0-beta.16",
    "electron-updater": "^5.3.0",
    "python-shell": "^5.0.0",
    "sqlite": "^4.1.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "electron": "23.1.1",
    "electron-builder": "^23.6.0"
  }
}
